# DEPRECATED

This folder used to contain a prototype / early Student Hub shell.
The real application is in the parent frontend app: `../src`.

Dev startup in this folder is intentionally disabled.

- Go up one level and work in `frontend/src/`.
- Run the app from `frontend/` (not from `frontend/apps/student-hub/`).
- Main dev URL: `http://127.0.0.1:5174/`
