# Implementation Blueprint v1.0 (Execution Baseline)

This repository follows the locked UDSM LMS Implementation Blueprint v1.0.

## Initial execution scope

- Sprint 0 setup:
  - monorepo scaffold
  - backend skeleton
  - frontend skeleton
  - local infra compose
- First implementation milestone:
  - health endpoint
  - bottom navigation shell
  - sync health card placeholder

## Replacement rule

The legacy file `lms.html` is superseded by the app under `frontend/apps/student-hub`.
